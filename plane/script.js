'use strict';

// Function in JS

const flight = 'AB2123';
const stardust = {
  name: 'Chandrashekhar',
  passport: 12356235,
};

const checkIn = function (flightNum, passenger) {
    
};

checkIn(flight, stardust);
